# DragonTCP
DragonTCP is a simple TCP messaging library. It is based on boost asio. Every data that is being sent needs to have an ID and a message. This is used for routing a specific purpose of a message to a specific function on either the client or server side.
